doctype html
html(lang="pt-br")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    
    //- Bootstrap 5 CSS (via CDN)
    link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css", rel="stylesheet", integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH", crossorigin="anonymous")
    
    //- Usando a variável 'titulo' passada pelo Express
    title= titulo

    style.
      body {
        background-color: #f8f9fa; /* Um cinza claro de fundo do Bootstrap */
      }
      .card-title strong { /* Estilo para o nome do item dentro do card */
        color: #0d6efd; /* Azul padrão do Bootstrap */
      }

  body
    //- Container principal do Bootstrap com margem no topo (mt-4)
    .container.mt-4

      //- Título da página com estilo de display e margem inferior (mb-4)
      h1.display-5.mb-4= titulo

      //- Verificando se o array 'itensParaView' existe e tem elementos
      if itensParaView && itensParaView.length > 0
        .row
          //- Iterando sobre o array 'itensParaView'
          each item in itensParaView
            //- Usando um Card do Bootstrap para cada item
            //- mb-3 adiciona margem inferior entre os cards
            .col-md-6.col-lg-4.mb-3
              .card.h-100
                .card-body.d-flex.flex-column
                  h5.card-title
                    strong= item.nome
                    small.text-muted.fw-normal ID: #{item.id}
                  p.card-text.flex-grow-1= item.descricao
                  a.btn.btn-sm.btn-outline-primary.mt-auto(href="#") Ver Detalhes

      else
        //- Mensagem caso o array esteja vazio, usando um Alert do Bootstrap
        .alert.alert-warning(role="alert")
          | Nenhum item para exibir no momento.

      hr.my-4

      //- Link para voltar, estilizado como botão Bootstrap
      a.btn.btn-secondary(href="/") Voltar para a página inicial

    //- Bootstrap 5 Bundle JS (inclui Popper) (via CDN) - Colocar no final do body
    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js", integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz", crossorigin="anonymous")
