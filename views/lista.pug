<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <!-- Viewport essencial para responsividade -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Bootstrap 5 CSS (via CDN) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <!-- Usando a variável 'titulo' passada pelo Express -->
    <title><%= titulo %></title>

    <style>
        /* Estilos personalizados podem ser adicionados aqui, se necessário,
           mas vamos priorizar as classes do Bootstrap */
        body {
            background-color: #f8f9fa; /* Um cinza claro de fundo do Bootstrap */
        }
        .card-title strong { /* Estilo para o nome do item dentro do card */
             color: #0d6efd; /* Azul padrão do Bootstrap */
        }
    </style>
</head>
<body>
    <!-- Container principal do Bootstrap com margem no topo (mt-4) -->
    <div class="container mt-4">

        <!-- Título da página com estilo de display e margem inferior (mb-4) -->
        <h1 class="display-5 mb-4"><%= titulo %></h1>

        <!-- Verificando se o array 'itensParaView' existe e tem elementos -->
        <% if (itensParaView && itensParaView.length > 0) { %>
            <div class="row"> <!-- Opcional: usar row/col para layout futuro, mas cards já são blocos -->
                <!-- Iterando sobre o array 'itensParaView' -->
                <% itensParaView.forEach(item => { %>
                    <!-- Usando um Card do Bootstrap para cada item -->
                    <!-- mb-3 adiciona margem inferior entre os cards -->
                    <div class="col-md-6 col-lg-4 mb-3"> <!-- Colunas para layout em grid (opcional, mas bom para responsividade) -->
                       <div class="card h-100"> <!-- h-100 faz os cards terem a mesma altura na linha -->
                           <div class="card-body d-flex flex-column"> <!-- d-flex e flex-column para alinhar conteúdo -->
                               <h5 class="card-title">
                                   <strong><%= item.nome %></strong>
                                   <small class="text-muted fw-normal"> (ID: <%= item.id %>)</small> <!-- text-muted para ID mais sutil -->
                               </h5>
                               <p class="card-text flex-grow-1"><%= item.descricao %></p> <!-- flex-grow-1 para empurrar o botão/link para baixo -->
                               <a href="#" class="btn btn-sm btn-outline-primary mt-auto">Ver Detalhes</a> <!-- Exemplo de botão/link no card -->
                           </div>
                       </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <!-- Mensagem caso o array esteja vazio, usando um Alert do Bootstrap -->
            <div class="alert alert-warning" role="alert">
                Nenhum item para exibir no momento.
            </div>
        <% } %>

        <hr class="my-4"> <!-- Linha divisória com margem vertical (my-4) -->

        <!-- Link para voltar, estilizado como botão Bootstrap -->
        <a href="/" class="btn btn-secondary">Voltar para a página inicial</a>

    </div> <!-- Fim do .container -->

    <!-- Bootstrap 5 Bundle JS (inclui Popper) (via CDN) - Colocar no final do body -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>